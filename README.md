# Dinesh Behera — Research Portfolio

A responsive personal research website designed for GitHub Pages.

## Before publishing

1. Replace every instance of `YOUR-USERNAME` in `index.html` with your GitHub username.
2. Replace every instance of `YOUR-EMAIL` with your preferred professional email.
3. Add your CV as `assets/Dinesh-Behera-CV.pdf`.
4. Add a profile photograph and replace the placeholder block in `index.html`.
5. Review the research and education text for accuracy and update dates as needed.

## Publish with GitHub Pages

### Recommended repository name

For the cleanest address, create a repository named:

```text
YOUR-USERNAME.github.io
```

Your site will then appear at:

```text
https://YOUR-USERNAME.github.io
```

### Upload method

1. Create the repository on GitHub.
2. Upload `index.html`, `styles.css`, `script.js`, and the `assets` folder.
3. Open **Settings → Pages**.
4. Under **Build and deployment**, select **Deploy from a branch**.
5. Select the `main` branch and `/ (root)` folder, then save.

## Local preview

Double-click `index.html`, or run:

```bash
python -m http.server 8000
```

Then open `http://localhost:8000`.

## Suggested future additions

- Publications and conference presentations
- Research project images
- Downloadable two-page industry CV
- Google Scholar and ORCID links
- A project page for spray-coated PSCs on CFRP
- A blog or research notes section
